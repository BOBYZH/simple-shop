<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>
    <%= webTitle %>
  </title>
  <link rel="Shortcut Icon" type="image/x-icon" href=<%= webIcon %>>
  <link href="https://bootswatch.com/5/<%= ThemeName %>/bootstrap.min.css" rel="stylesheet">
  <!-- Default theme: -->
  <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous"> -->
</head>

<body>

  <div class="d-flex flex-column flex-md-row justify-content-between align-items-center p-3 px-md-4 mb-3">

    <h5 class="my-0 mr-md-auto font-weight-normal">
      <a class="h3" href="/">
        <%= webTitle %>
      </a>
    </h5>

    <nav class="my-2 my-md-0 mr-md-3">

      <span class="p-2 text-dark d-none d-sm-inline">歡迎光臨，
        <%= userName || "新顧客" %>！
      </span>
      <a class="p-2 text-dark" href="/cart">購物車</a>

      <% if (!userName) { %>

        <a class="p-2 text-dark" href="/signin">登入會員</a>

        <% } else { %>

          <a class="p-2 text-dark" href="/orders">訂單查詢</a>
          <!-- 使用post形式提交避免游標碰到即登出 -->
          <form method="post" action="logout" class="d-inline-block">
            <button type="submit" class="p-2 text-dark">
              登出
            </button>
          </form>

          <% } %>

    </nav>
  </div>


  <main class="container-fluid">
    <% if (successMessages.length !==0) { %>
      <div class="alert alert-success my-5 py-3 text-center" role="alert">
        <%- successMessages %>
      </div>
      <% } %>

        <% if (errorMessages.length !==0) { %>
          <div class="alert alert-danger my-5 py-3 text-center" role="alert">
            <%- errorMessages %>
          </div>
          <% } %>

            <%- body -%>

  </main>
</body>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
  integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

</html>